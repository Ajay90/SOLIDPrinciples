public class AccountFixed{ // Fields and accessors omitted.

  public double calculateInterest(InterestCalculation interestCalculation){
    return interestCalculation.calculateInterest(balance);
  }
  
}
